# Parameters
nsim<-250  ## simulation times
n<-100     ## number of clusters
m<-25      ## cluster size or number of individuals in each cluster
alpha<-1   ## intercept in the mean model
beta<-0.4           ## intervention effect
gamma<-1            ## age effect
p<-0.5              ## the parameter to generate age group
sigmai<-4           ## the variance of each individual
rho1<-0.1           ## the correlation between individuals in the same age group (larger)
rho2<-0.01          ## the correlation between individuals in different age group (smaller)


